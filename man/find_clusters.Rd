% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common.R
\name{find_clusters}
\alias{find_clusters}
\title{Tract Clustering}
\usage{
find_clusters(distance_vector, k = 1L, max_iter = 100L,
  nstart = 100L, ncores = 1L)
}
\arguments{
\item{k}{An integer specifying the number of clusters (default: \code{1L}).}

\item{max_iter}{An integer specifying the maximum number of iterations
(default: \code{100L}).}

\item{nstart}{An integer specifying the number of random initializations
(default: \code{100L}).}

\item{ncores}{An integer specifying the number of cores available for the computations (default: \code{1L}).}

\item{distance_matrix}{A matrix of size \code{n x n} where \code{n} is the
number of streamlines in the tract and each entry of the matrix evaluates
the distance between two streamlines.}
}
\value{
A list with two components: \code{groups} gives membership of each
  individual streamline while \code{centroids} gives the labels of the
  groups' centroids.
}
\description{
Tract Clustering
}
\examples{
D <- matrix(runif(100), 10L, 10L)
D <- (D + t(D)) / 2
diag(D) <- 0
find_clusters(D, k = 4L)
}
